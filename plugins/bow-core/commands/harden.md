---
description: Security-harden app-layer code (input, auth, secrets)
argument-hint: [file, feature, or surface]
---

Apply the **security-and-hardening** skill to: $ARGUMENTS

Focus on app-layer risks: input validation, auth/session, secret handling, and untrusted data at
trust boundaries. For database-layer concerns (RLS, views, triggers, SQL), defer to a dedicated DB
security review skill instead.
