---
description: Find the root cause of a failure systematically
argument-hint: [error, failing test, or symptom]
---

Apply the **debugging-and-error-recovery** skill to: $ARGUMENTS

Reproduce first, form a hypothesis, isolate the root cause across the full stack (app, database
constraints/triggers, edge/serverless, web), and verify the live runtime path — not just static
green — before declaring it fixed.
