---
description: Break a spec or requirement into ordered, implementable tasks
argument-hint: [spec path or requirement]
---

Apply the **planning-and-task-breakdown** skill to: $ARGUMENTS

Produce ordered, atomic tasks (one feature branch per unit), call out dependencies and what can
run in parallel, and estimate scope. Do not start implementing — output the plan for review.
